============
Installation
============

At the command line::

    $ easy_install mailgun-requests

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mailgun-requests
    $ pip install mailgun-requests
